import React, { Component } from 'react';
import Login from './Login.js';
import { View, Text } from 'react-native';
export default class Ethereal extends Component {
  render() {
    return (
      <Login/>
    );
  }
};
